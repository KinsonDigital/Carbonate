name: ✅ SDK Status Check
run-name: ✅ SDK Status Check (${{ github.head_ref }} branch)


permissions:
  contents: read


defaults:
  run:
    shell: pwsh


on:
  pull_request:
    branches: [main, preview]


jobs:
  sdk_status_check:
    name: ${{ vars.PROJECT_NAME }} SDK Status Check
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Validate SDK Versions
        uses: KinsonDigital/Infrastructure/actions/validate-sdk-versions@5dade832543507f6ee8f9123794ca7dd3b317e67 # v19.0.1
        with:
          base-search-dir-path: "${{ github.workspace }}"
          net-sdk-version: "${{ vars.NET_SDK_VERSION }}"
